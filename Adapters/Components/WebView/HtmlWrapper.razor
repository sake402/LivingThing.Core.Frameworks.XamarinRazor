@using Microsoft.AspNetCore.Components.Web
@using Microsoft.AspNetCore.Components.Authorization

<html lang="en">
<head>
    @if (BaseUrl != null)
    {
    <base href="@BaseUrl" />
    }
    @Head
</head>
<body>
    <CascadingAuthenticationState>
        @ChildContent
        @*<RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />*@
    </CascadingAuthenticationState>
    <!--<Router AppAssembly="@typeof(MainLayout).Assembly" AdditionalAssemblies="AdditionalAssemblies">
        <Found Context="routeData">-->
    @*<CascadingAuthenticationState>
            <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
        </CascadingAuthenticationState>*@
    <!--<AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    <div class="text-center pd-10">
                        <i class="mdi mdi-stop fsz-100 cl-fatal"></i>
                        <h1 class="op-6">Access Denied!!!</h1>
                        <p class="fsz-20 op-6">You are not authorized to view that page.</p>
                    </div>
                </NotAuthorized>
                <Authorizing>
                    <div class="text-center pd-10">
                        <i class="mdi mdi-loading mdi-spin fsz-100 cl-success"></i>
                        <h1 class="op-6">Authorizing...</h1>
                        <p class="fsz-20 op-6">This will only take a moment.</p>
                    </div>
                </Authorizing>
            </AuthorizeRouteView>
        </Found>
        <NotFound>
            <LayoutView Layout="@typeof(MainLayout)">
                <p>Sorry, there's nothing at this address.</p>
            </LayoutView>
        </NotFound>
    </Router>-->
    @{
        MarkupString script = new MarkupString($"<script static>{Script}</script>");
        //MarkupString script = new MarkupString($"<script src=\"file:///assets/WebView.js\"></script>");
    }
    @script
</body>
</html>
